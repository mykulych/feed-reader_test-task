import { Layout } from "../components/layout/Layout"
import { PostsList } from "../components/postsList/PostsList"

export const Posts = () => {
    return <Layout>
        <PostsList />
    </Layout>
}