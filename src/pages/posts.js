import { Layout } from "../components/layout/Layout"
import { PostsContainer } from "../containers/PostsContainer"

export const Posts = () => {
    return <Layout>
        <PostsContainer />
    </Layout>
}