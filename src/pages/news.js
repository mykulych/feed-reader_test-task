import { Layout } from "../components/layout/Layout";
import { NewsList } from "../components/newsList/NewsList";

export const News = () => {
  return (
    <Layout>
      <NewsList />
    </Layout>
  );
};
